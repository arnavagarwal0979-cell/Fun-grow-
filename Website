<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Funngro Revamp | Smart Website</title>
    <!-- Google Fonts for that premium serif look -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-color: #050e09; /* Deep dark green from 733.jpg */
            --primary-green: #00d084; /* Signature Funngro green */
            --card-bg: #0a1610;
            --text-main: #ffffff;
            --text-dim: #a0a0a0;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-main);
            font-family: 'Inter', sans-serif;
            margin: 0;
            line-height: 1.6;
        }

        /* Hero / Header Inspired by 733.jpg */
        .container { padding: 40px 20px; max-width: 600px; margin: auto; }
        
        .badge {
            text-transform: uppercase;
            letter-spacing: 2px;
            font-size: 10px;
            color: var(--primary-green);
            display: block;
            margin-bottom: 20px;
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: 2.8rem;
            line-height: 1.1;
            margin-bottom: 20px;
        }

        h1 em {
            color: var(--primary-green);
            font-style: italic;
        }

        p.subtitle {
            color: var(--text-dim);
            font-size: 1.1rem;
            margin-bottom: 40px;
        }

        /* Buttons from 733.jpg */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 15px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: 600;
            margin-bottom: 15px;
            width: 100%;
            transition: 0.3s;
        }

        .btn-primary { background-color: var(--primary-green); color: #000; }
        .btn-outline { border: 1px solid var(--text-dim); color: var(--text-main); }

        /* Step Cards Inspired by 735.jpg */
        .step-card {
            background-color: var(--card-bg);
            border: 1px solid #1a2a22;
            padding: 25px;
            margin-bottom: 15px;
            display: flex;
            align-items: flex-start;
            border-radius: 4px;
        }

        .step-num {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--primary-green);
            font-style: italic;
            margin-right: 20px;
        }

        .step-content h3 { margin: 0 0 10px 0; font-size: 1.1rem; font-weight: 400; }
        .step-content p { margin: 0; color: var(--text-dim); font-size: 0.9rem; }

        /* Requirements Box from 734.jpg */
        .req-box {
            border: 1px solid #1a2a22;
            padding: 30px;
            margin: 40px 0;
        }
        .req-box ul { list-style: none; padding: 0; }
        .req-box li { color: var(--text-dim); margin-bottom: 10px; }
        .req-box li::before { content: "— "; color: var(--primary-green); }

    </style>
</head>
<body>

<div class="container">
    <!-- Header Section -->
    <span class="badge">• CHAPTER ONE • FIVE STEPS •</span>
    <h1>Five steps from first tap to <em>first payout.</em></h1>
    <p class="subtitle">Most users earn their first rupee within twenty-four hours. Here is precisely how it happens, step by step.</p>

    <a href="#" class="btn btn-primary">Download app &rarr;</a>
    <a href="#" class="btn btn-outline">See real brand work &rarr;</a>

    <!-- Requirements Section (from 734.jpg) -->
    <div class="req-box">
        <span class="badge" style="font-size: 8px;">WHAT YOU NEED</span>
        <ul>
            <li>A device with internet</li>
            <li>A UPI ID for payouts</li>
            <li>Two minutes for sign-up</li>
        </ul>
    </div>

    <!-- Step Progression (from 735.jpg) -->
    <span class="badge" style="margin-top: 50px;">• YOUR 7-STEP PROGRESSION •</span>
    
    <div class="step-card">
        <div class="step-num">1</div>
        <div class="step-content">
            <h3>Explore brands & products</h3>
            <p>Find what interests you across 5,000+ brands.</p>
        </div>
    </div>

    <div class="step-card">
        <div class="step-num">2</div>
        <div class="step-content">
            <h3>Share feedback & insights</h3>
            <p>Surveys and product feedback, your first earning category.</p>
        </div>
    </div>

    <div class="step-card">
        <div class="step-num">3</div>
        <div class="step-content">
            <h3>Influence your peers</h3>
            <p>Referrals unlock once you've completed your first tasks.</p>
        </div>
    </div>
</div>

</body>
</html>
